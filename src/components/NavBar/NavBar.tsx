import React from "react";
import styles from "./NavBar.module.scss";

const NavBar = () => {
  return (
    <nav>
      <div className={styles.title}>O R B</div>
    </nav>
  );
};

export default NavBar;
